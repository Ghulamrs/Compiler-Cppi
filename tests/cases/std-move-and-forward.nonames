x86_64-linux  there is nothing to compare here, by construction: a case that includes a C++ standard library header gets clang's libc++ names for move, forward, reverse and their helpers, and cxx1 its own <utility> and <algorithm>; the runs agree, which run.sh checks.
x86_64-windows  there is nothing to compare here, by construction: a case that includes a C++ standard library header gets clang's libc++ names for move, forward, reverse and their helpers, and cxx1 its own <utility> and <algorithm>; the runs agree, which run.sh checks.
arm64-darwin  there is nothing to compare here, by construction: a case that includes a C++ standard library header gets clang's libc++ names for move, forward, reverse and their helpers, and cxx1 its own <utility> and <algorithm>; the runs agree, which run.sh checks.
