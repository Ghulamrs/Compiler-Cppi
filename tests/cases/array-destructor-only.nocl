cl destroys the arrays of Dd and Vd from `delete[]` through their vector deleting destructors, `??_EDd@@QEAAPEAXI@Z` and `??_EVd@@UEAAPEAXI@Z`, where cxx1 loops the destructor in a file-local function and frees in the open, and names Vd's scalar deleting destructor `??_GVd@@UEAAPEAXI@Z` for its vftable where cl reaches it through the vector one; the shape array-of-class.nocl and array-of-class-destructor.nocl record. Every name the program declares agrees.
