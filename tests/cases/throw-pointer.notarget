x86_64-windows  a pointer-typed throw wants the Microsoft ThrowInfo chain for a pointer - measured from cl: ??_R0PEAH@8 with '.PEAH', a _CT per catchable type, a _CTA listing the pointer, its base pointers and PEAX for void *, and _TI2PEAH, with the C in _TI2CPEAD saying the pointee was const - which is the same step the class-typed throw still waits for on this target (throw-class.notarget). Refused by name here as that is.
